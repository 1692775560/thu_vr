# Wanda/Jetson 部署说明

部署目录：`/home/unix_ai/thu_vr`

## 从机器人头部相机识别一次

```bash
cd /home/unix_ai/thu_vr
./run_robot_ocr.sh
```

脚本只订阅 `/head_rgbd/color/image_raw`，不会发布控制指令或让机器人运动。
每次运行的原图、标注图和 `result.json` 保存在 `runs/<时间戳>/`。

如需使用其他图像话题：

```bash
CAMERA_TOPIC=/other/image_topic ./run_robot_ocr.sh
```

## 识别已有图片或视频

```bash
cd /home/unix_ai/thu_vr
.venv/bin/python real_scene_ocr.py /path/to/input --no-easy --output-dir runs/manual
```

当前部署启用 RapidOCR 路线。YOLO 路线需要额外的 `best.pt` 权重，仓库中未包含该模型，因此未作为默认入口。

## 双相机实时网页

网页同时显示头部和底部相机的原始画面、最新 OCR 标注画面：

```bash
http://192.168.137.212:5090/
```

原画从现有 5000 端口相机网页的内存快照低频取样（默认每路 2 FPS）；头部和底部由两个独立 OCR worker 并行处理，始终丢弃积压帧、只处理最新帧。当前真机稳定段单帧约 4.7–6.2 秒。

头部 Gemini 2 使用 `libuvc` 和 MJPG 输出，避免 `v4l2` 在 1920×1080 帧中产生水平错位接缝。OCR 会优先使用白色标签轮廓作为稳定裁剪框；编号行批量识别与标签区域审计 OCR 并行运行，同一行多个标签的上半部会经过对齐和中值叠加后动态识别重复前缀。生产服务不配置固定行前缀或编号范围：前缀与任意 `0000–9999` 连续编号范围都从像素学习，完整行建立的序列可用于对齐当前只看到部分标签的相邻行和另一相机。最近 5 帧只是滚动缓存上限，正常异常确认使用 2 帧；常见的一字符混淆需要 3 次极高置信证据，避免把 `0010/0020` 或 `A01/A00` 的 OCR 抖动报成错误标签。

`thu-vr-camera-web.service` 不再建立第二份 ROS 高清图像订阅，仅通过 localhost 读取现有相机网页已经缓存的 JPEG 快照。它不会启动、停止或重启任何相机，不发布机器人运动指令，也不写入录制程序。系统服务设置了低调度优先级、3 核总 CPU 上限、每个实时 worker 2 个主 OCR 计算线程和 4 GB 内存上限；进程意外正常退出或失败都会由 systemd 自动拉起。

## OCR 消融实验

5090 网页底部的“消融实验采样”区域会为每个条件同时保存头部和底部相机原图。默认每路 10 帧（探索实验），可在网页选择 3–30 帧；正式统计建议每路 30 帧。固定真值为从上到下 `A05`、`A04`、`A03`、`A02`、`A01`，每行 `0010` 到 `0020`。采样时填写实测距离，并为头部/底部相机分别勾选实际可见行；程序自动记录头部实际关节角、底盘 odom 位姿、相机元数据和时间戳。正式数据保存在：

```text
/home/unix_ai/thu_vr/ablation_data/<trial_id>/
```

采样完成后，网页服务会自动将 trial 放入单任务、最低调度优先级分析队列。记录会立即出现在网页中，并每 2 秒更新排队状态、逐方案进度和部分帧汇总；完成后按“相机 → 标签行”分别显示识别率、整帧完整率、OCR 置信度、精确率和耗时。分析子进程只使用 1 个 OCR 线程并继承服务的 CPU/内存限制，不会并发运行多个 trial。

每个相机结果还包含“逐标签审计”。对每个预期标签分别统计正确帧、误读帧和漏检帧，并保留误读候选区域的中心坐标、原始 OCR 文本及多余预测。位置真值来自采样时人工选择的可见行和实验板从左到右的编号顺序；如果人工可见行选择错误，对应审计也会失真。真实现场若没有外部标签清单或人工标注，只能做格式、置信度和跨帧一致性检查，不能用 OCR 自身输出证明其语义真值。

如需手工重新运行同一批帧：

```bash
cd /home/unix_ai/thu_vr
THU_VR_OCR_THREADS=2 nice -n 15 .venv/bin/python ablation_runner.py ablation_data/<trial_id>
```

每个 trial 会生成 `ablation_results.json` 和 `ablation_summary.csv`。可部署方案包括整帧原图基线、候选标签彩色/灰度/CLAHE/CLAHE+锐化/自适应二值化、图像内动态行前缀共识、行前缀像素叠加，以及按标签位置跟踪的字符级跨帧融合。人工已知行前缀和固定编号顺序被明确标为“实验上限（不可部署）”，不参与现场方案选型；严格 OCR、动态图像证据和人工真值先验分别报告。
